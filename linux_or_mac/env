#!/usr/bin/env bash


VERSION="1.13.12"

CONFIG_URL="网址"
PORT=443
USERNAME="用户名"
PASSWORD="用户密码"
KEY="解密密钥"

DURATION=72    # in hours


GetExeUrl(){
	local version=$1
	local kernel=$2
	local arch=$3
	echo "https://github.com/SagerNet/sing-box/releases/download/v$version/sing-box-$version-$kernel-$arch.tar.gz"
}